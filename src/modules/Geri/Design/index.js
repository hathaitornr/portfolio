import React from "react"
import { Section } from "../../../components"
import styles from "../../../styles/project.module.scss"

const GeriDesign = () => {
  return (
    <Section title="Design">
      <Section title="Ideation" sectionStyle="sub">
        Section for Ideation
      </Section>
      <Section title="Product Loop" sectionStyle="sub">
        Section for Product Loop
      </Section>
      <Section title="Prototype" sectionStyle="sub">
        Section for Prototype
      </Section>
    </Section>
  )
}

export default GeriDesign
